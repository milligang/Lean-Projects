\begin{tikzpicture}[every node/.style={draw,circle, thick, minimum size=8mm}]
  \graph[clockwise, radius=3cm,     typeset=\pgfmathparse{int(\tikzgraphnodetext-1)}\pgfmathresult
  ] {
    subgraph C_n [n=5,name=A]
  };

  \graph[clockwise, radius=1.5cm, 
  typeset=\pgfmathparse{int(\tikzgraphnodetext-1)}\pgfmathresult'] {
  subgraph I_n [n=5,name=B]
  };

  \foreach \i [evaluate={\j=int(mod(\i+2+4,5)+1)}]
     in {1,2,3,4,5}{
    \draw (A \i) -- (B \i);
    \draw (B \j) -- (B \i);
  }
\end{tikzpicture}